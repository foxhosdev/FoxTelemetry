<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <provider
            android:name="com.foxtelemetry.init.FoxTelemetryInitProvider"
            android:authorities="${applicationId}.foxtelemetry-init"
            android:exported="false"
            android:initOrder="100" />
    </application>
</manifest>
